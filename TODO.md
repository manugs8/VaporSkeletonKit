# Future Improvements

This document tracks architectural considerations and enhancements for future major or minor releases.

Nothing pending right now. See `CHANGELOG.md` for what has shipped, including the
customizable fault body for `TestFaultInjectionMiddleware`/`armFault` this document used
to track.

Related: `manugs8/WorkOSBearerAuth`'s `TODO.md` (§4) tracks the sibling gap for
auth-focused E2E scenarios under `.testing`.
